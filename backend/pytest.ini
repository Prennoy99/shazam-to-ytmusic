[pytest]
# Put the backend package root on sys.path so `from app import ...` resolves.
# Without this, a bare `pytest` fails at collection with "No module named 'app'"
# — only `python -m pytest` happens to work, because that form adds the current
# directory to sys.path and a bare `pytest` does not.
pythonpath = .
testpaths = tests
